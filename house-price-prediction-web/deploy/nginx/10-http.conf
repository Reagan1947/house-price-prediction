# HTTP：统一跳转到 HTTPS（TLS 由 deploy/nginx/20-https.conf 与 deploy/certs/ 提供）。

server {
    listen 80;
    server_name _;

    client_max_body_size 25m;

    location / {
        return 301 https://$host$request_uri;
    }
}
